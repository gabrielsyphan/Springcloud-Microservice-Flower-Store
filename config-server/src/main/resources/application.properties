server.port=8888
spring.cloud.config.server.git.uri=https://github.com/gabrielsyphan/springcloud-microservices
spring.cloud.config.server.git.search-paths=config-repo
# If you want to set a local folder as config repo, you can use the following line instead of the above line
# spring.profiles.active=native
# spring.cloud.config.server.native.search-locations=file://${user.home}/config-repo
